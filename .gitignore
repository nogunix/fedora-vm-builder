# Terraform working files. The playbooks run Terraform in vm_tf_dir, but these
# also appear here whenever terraform is run directly against terraform/.
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log

# Generated by the create playbook from vars.yml; contains vm_password.
terraform.tfvars.json

# Ansible
*.retry
.ansible/
